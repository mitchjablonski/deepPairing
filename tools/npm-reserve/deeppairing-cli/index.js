#!/usr/bin/env node
/* eslint-disable */
process.stderr.write(
  "\n  This is a typosquat-defense placeholder. The CLI ships inside the `deeppairing` package — there is no separate -cli sub-package.\n" +
  "  Real project: https://github.com/deeppairing/deeppairing\n\n",
);
process.exit(1);
