# E1 — claude-plugin/server/ is GENERATED (bundle-plugin.mjs) but COMMITTED:
# the marketplace source is this git repo, so gitignoring the bundle meant
# marketplace installs had no server at all (E1 review blocker). CI gates
# staleness: the check job rebuilds and fails on `git diff claude-plugin/server`.
