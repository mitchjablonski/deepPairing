---
description: Review past deepPairing sessions — search for a concept, artifact, or pattern across every session in this project
argument-hint: [query]
---

Run `recall` with query: $ARGUMENTS, mode: "sessions"

Summarize the top 5 matches with:
- Artifact title and type
- Which session it's from
- What `matchedVia` tags hit (title / concept / content / rejected)
- A one-sentence takeaway

If no matches, tell me and suggest a different query. If several matches
look relevant, ask which I'd like to dig into via the MCP resource
`deeppairing://artifact/{id}`.
